We assume that you have access to a running instance of *Rafiki Admin* at ``<admin_host>:<admin_port>``. 
If you are setting up Rafiki on your local machine, refer to :ref:`setup-development`.

1. Install Python 3.6

2. Clone the project at https://github.com/nginyc/rafiki.

3. Within the project's root folder, install Rafiki Client's Python dependencies by running:

    .. code-block:: shell

        pip install -r ./rafiki/client/requirements.txt